# Put the project root on Python's path, so that `import src` works with the
# plain `pytest` command - the one the GitHub workflow runs - and not only
# with `python -m pytest`. The first CI run failed on exactly this difference.
[pytest]
pythonpath = .
testpaths = tests
